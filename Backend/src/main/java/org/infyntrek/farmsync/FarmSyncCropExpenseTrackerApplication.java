package org.infyntrek.farmsync;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Main Spring Boot entry point for the FarmSync backend.
 */
@SpringBootApplication
public class FarmSyncCropExpenseTrackerApplication {

    public static void main(String[] args) {
        SpringApplication.run(FarmSyncCropExpenseTrackerApplication.class, args);
    }
}
